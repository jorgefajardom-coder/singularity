node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.env
.env.*
!.env.example

# Archivos fuente de Blender (opcional: quítalo si quieres versionarlos)
*.blend1

__pycache__/
*.pyc

# Originales pesados del astronauta (5,7 MB entre los cinco). El sitio usa el
# .webp derivado, que pesa 128 kB. Se quedan en disco para poder regenerarlo
# o probar otra pose, pero no viajan al repositorio.
public/images/about-astronaut*.png

# Notas de trabajo, no van al repositorio publico
README.md

# Modelo de partida de la celda (33 MB). De aqui sale, con
# tools/split-celda-pieces.mjs, el celda-pieces.glb que sirve el sitio.
models-src/

# Resultados de Playwright (las capturas de referencia SI se versionan:
# tests/e2e/*-snapshots/)
test-results/
playwright-report/
